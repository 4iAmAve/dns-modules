{
  "name": "@datns/node-ts-mock-server",
  "version": "0.2.3",
  "main": "dist/server.js",
  "files": [
    "build"
  ],
  "scripts": {
    "start": "node dist/server.js",
    "build": "./node_modules/.bin/tsc && cp -r sslcert/ dist/sslcert",
    "clean-build": "rm -rf build",
    "lint": "tslint --exclude **/*.d.ts app/**/*.ts"
  },
  "repository": "https://github.com/4iAmAve/dns-modules/tree/master/packages/dns-node-ts-mock-server",
  "author": "Ave <me@aves.site>",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.11.1",
    "typescript": "^3.2.2"
  }
}
